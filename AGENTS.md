# Expo HAS CHANGED

This project is on SDK 55 (`expo` in `package.json` is the authority). Read the
exact versioned docs at https://docs.expo.dev/versions/v55.0.0/ before writing
any code, and update this line when the SDK is bumped.
