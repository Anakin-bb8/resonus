# Expo HAS CHANGED

This project is on SDK 56 (`expo` in `package.json` is the authority). Read the
exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing
any code, and update this line when the SDK is bumped.
